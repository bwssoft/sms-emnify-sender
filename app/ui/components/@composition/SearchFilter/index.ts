import { Modal } from '../../Modal';
import { ComponentApp } from './Component';
import { Root } from './Root';
import Trigger from './Trigger';

export const SearchFilter = Object.assign(ComponentApp, {
  Root,
	Trigger,
  Modal,
})
